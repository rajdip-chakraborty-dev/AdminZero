# Git Authorship Rules for AI Agents

When executing Git commands or drafting commit messages for this repository, you must adhere strictly to the following rules:
1. NEVER append or use the `--author` flag in any Git command.
2. DO NOT add "Co-authored-by" trailers to commit messages.
3. Rely entirely on the existing local system Git configurations for all authorship data.